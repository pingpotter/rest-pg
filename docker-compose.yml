version: "3.7"

services:
  api:
    build:
        context: .
        dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment: 
      - DB_DSN=postgres://postgres:password@192.168.0.70:5432/rest-pg
